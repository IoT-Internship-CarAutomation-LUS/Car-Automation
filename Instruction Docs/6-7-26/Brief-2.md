Brief 2 — ELM327 + TPMS Parts & Data-Path Research *DOC* (→ Shaahir leads, the three assist on sourcing)Context (read first, non-negotiable): Everything must be in-house — the ELM327's own Bluetooth phone-app is not an option (the lead won't sign off on it). So we are not researching Bluetooth-app setups. We're buying an ELM327 and connecting it into our own hardware. This brief is about which unit and how it physically connects to our system.Part A — Which ELM327 to buy. Compare 3–4 specific ELM327 units available in India (Amazon.in / local electronics vendors), in a table:
Unit / seller / price
Connection type — USB, or WiFi, or Bluetooth (we want USB/wired, so we can wire it to a laptop or ESP32; flag which units are USB)
Chip — genuine FTDI vs clone (CH340 etc.). Genuine FTDI USB is strongly preferred — the cheap clones are a known cause of "not working properly," which is likely what killed our first unit.
OBD-II protocols supported — must support ISO 15765 (CAN)
Source link
Recommendation — the single best USB unit to buy, with reasoning.
Part B — The data path (this is the important half — Shaahir owns this). Answer, in writing, how the vehicle data physically gets from the car to our server, for our in-house setup. Specifically address these two candidate architectures and pick one:
Option 1 — ELM327 (USB) → laptop in the car → server. The laptop runs our own PID-decode code and sends the 32-byte packet to api.nalusa.space over its own internet. Question to answer: is putting a laptop in the car acceptable/practical for the validation test, and how does the laptop reach the server (car WiFi hotspot? phone tether?)?
Option 2 — ELM327 (USB/UART) → ESP32 in the car → server. The ESP32 decodes and sends over its own WiFi/GSM (we already have the SIM800L on a board, currently parked). This is more in-house and needs no laptop. Question to answer: can our ESP32 talk to a USB ELM327 (may need USB-host or a UART-level ELM327), and is the GSM/WiFi link reliable enough to stream?
Deliver: a recommendation of Option 1 or 2 (or a hybrid), with an honest note on what each needs and its risks. This is a judgment answer, not just a table — that's why Shaahir owns it, not the three.Part C — TPMS (sourcing, the three can do this). We're installing our own aftermarket 433MHz valve-cap TPMS sensors (set of 4) + a 433MHz receiver module for the ESP32 to decode. Find:
2–3 aftermarket external TPMS sensor kits (set of 4, valve-cap type, 433MHz) available in India — unit, price, source link
433MHz RF receiver module options (e.g. superheterodyne SRX882 or similar) that an ESP32 can read — unit, price, source link
Critical flag: note that different TPMS brands use different packet formats, so whichever kit we pick determines the decode work later. Flag which kits have any documented/known packet format (some are reverse-engineered online) vs. fully proprietary — this affects how hard the decode will be.
Verification (you + Shaahir): Part A and C are checkable against source links (the three + Shaahir). Part B is the judgment call — Shaahir drafts it, you make the final architecture decision, since it affects the whole acquisition design.